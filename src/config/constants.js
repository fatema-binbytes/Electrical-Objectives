const API_URL = 'https://electric.binbytes.com/api';

const KEY_AUTH = 'AUTH';

const KEY_DONT_ASK = 'DONT_ASK';
const KEY_OPEN_COUNT = 'OPEN_COUNT';

const defaultConfig = {
  API_URL,
  KEY_AUTH,
  KEY_DONT_ASK,
  KEY_OPEN_COUNT,
};

export default {
  ...defaultConfig,
};
